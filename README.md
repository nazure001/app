# App Monorepo

Monorepo starter menggunakan:

- pnpm workspace
- Turborepo
- Next.js untuk web
- Express untuk API

## Struktur

- `apps/web` → frontend
- `services/api` → backend
- `packages/utils` → shared utilities

## Menjalankan project

```bash
pnpm install
pnpm dev